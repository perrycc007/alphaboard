import { Bar } from '../../../common/types';
export declare function emaGapSeries(bars: Bar[], fast: number, slow: number): number[];
export declare function isConverging(series: number[], lookback: number): boolean;
